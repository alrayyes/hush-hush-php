/vendor/

# phpDocumentor build output
/site/

# Regenerated by the pact-consumer CI job on every run; published as a
# build artifact, not committed.
/pact/pacts/

# Local tool caches
.php-cs-fixer.cache
.phpunit.result.cache
.phpunit.cache/
.phpdoc/

# Written by infection on every run; uploaded as a build artifact by
# mutation.yml's scheduled job, not committed.
/infection.log

node_modules/
.bun-cache/

# Written by the release job, not by hand.
CHANGELOG.md
